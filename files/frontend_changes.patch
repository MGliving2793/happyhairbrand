diff --git "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\frontend\\src\\Checkout.jsx" "b/C:\\Users\\HP\\Documents\\final web\\frontend\\src\\Checkout.jsx"
index 420e2e0..bc000b8 100644
--- "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\frontend\\src\\Checkout.jsx"	
+++ "b/C:\\Users\\HP\\Documents\\final web\\frontend\\src\\Checkout.jsx"	
@@ -349,6 +349,9 @@ export default function Checkout({ isOpen, onClose, initialProduct }) {
                         <span className="z-payment-badge">UPI Intent</span>
                       </div>
                       <div className="z-payment-desc">Open your UPI app to complete payment.</div>
+                      <div style={{ marginTop: 8 }}>
+                        <button type="button" onClick={() => { try { window.open('/api/debug/qr-scanner', '_blank'); } catch(e){} }} style={{ padding: '8px 10px', borderRadius: 8, border: 'none', background: '#eee', color: '#111', cursor: 'pointer' }}>Open QR Scanner</button>
+                      </div>
                     </div>
                   </label>
 
diff --git "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\frontend\\src\\UPIQr.jsx" "b/C:\\Users\\HP\\Documents\\final web\\frontend\\src\\UPIQr.jsx"
index 94a4ea0..5e0a812 100644
--- "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\frontend\\src\\UPIQr.jsx"	
+++ "b/C:\\Users\\HP\\Documents\\final web\\frontend\\src\\UPIQr.jsx"	
@@ -1,3 +1,26 @@
-// UPIQr component removed — replaced with noop placeholder to avoid removed-file errors.
 import React from 'react';
-export default function UPIQr() { return null; }
+
+// Simple UPI QR helper: opens server UPI QR page in a new tab and provides a scanner link
+export default function UPIQr({ amount }) {
+  const openQr = () => {
+    const url = `/api/debug/upi-qr${amount ? `?amount=${encodeURIComponent(amount)}` : ''}`;
+    window.open(url, '_blank');
+  };
+
+  const openScanner = () => {
+    const url = `/api/debug/qr-scanner`;
+    window.open(url, '_blank');
+  };
+
+  return (
+    <div style={{ marginTop: 12, display: 'flex', gap: 8, alignItems: 'center' }}>
+      <button type="button" onClick={openQr} style={{ padding: '8px 12px', background: '#c99339', color: '#fff', borderRadius: 8, border: 'none', cursor: 'pointer' }}>
+        Open UPI QR
+      </button>
+      <button type="button" onClick={openScanner} style={{ padding: '8px 12px', background: '#eee', color: '#111', borderRadius: 8, border: 'none', cursor: 'pointer' }}>
+        Open QR Scanner
+      </button>
+      <div style={{ fontSize: 12, color: '#666' }}>(Opens in new tab)</div>
+    </div>
+  );
+}
diff --git "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\public\\js\\product-sync.js" "b/C:\\Users\\HP\\Documents\\final web\\public\\js\\product-sync.js"
index b49961d..95405f0 100644
--- "a/C:\\Users\\HP\\Documents\\final web\\temp_happyhair_clone\\public\\js\\product-sync.js"	
+++ "b/C:\\Users\\HP\\Documents\\final web\\public\\js\\product-sync.js"	
@@ -182,12 +182,18 @@
         </div>
         <div class="dyn-form-group">
           <label>Payment Method <span id="dyn-pay-discount-text" style="color:#2ecc71;font-size:10px;display:none;">(₹10 OFF applied)</span></label>
-          <select id="dyn-paymode" onchange="calculateDynTotal()">
+          <select id="dyn-paymode" onchange="calculateDynTotal(); toggleDynScanner();">
             <option value="UPI">UPI / Pay via UPI App</option>
             <option value="COD">Cash on Delivery (COD)</option>
           </select>
         </div>
         
+        <div id="dyn-upi-actions" style="margin-bottom:12px; display:flex; gap:8px; align-items:center;">
+          <button type="button" id="dyn-open-upi-qr" onclick="openDynUpiQr()" style="padding:8px 10px; border-radius:8px; background:#c99339; color:#fff; border:none; cursor:pointer;">Open UPI QR</button>
+          <button type="button" id="dyn-open-qr-scanner" onclick="openDynUpiScanner()" style="padding:8px 10px; border-radius:8px; background:#eee; color:#111; border:none; cursor:pointer;">Open QR Scanner</button>
+          <div style="font-size:12px;color:#ccc;">(opens in new tab)</div>
+        </div>
+
         <div style="background: rgba(0,0,0,0.3); padding: 12px; border-radius: 8px; margin-bottom: 15px; border: 1px solid rgba(255,255,255,0.05);">
           <div style="display:flex; justify-content:space-between; margin-bottom:6px; font-size:13px; color:#aaa;">
             <span>Subtotal:</span>
@@ -294,6 +300,7 @@
     // Reset inputs
     document.getElementById('dyn-paymode').value = 'UPI';
     calculateDynTotal();
+    toggleDynScanner();
     
     document.getElementById('dyn-checkout-modal').classList.add('open');
   };
@@ -358,6 +365,14 @@
       });
       const data = await res.json();
       if (res.ok) {
+        // Keep modal open briefly for UPI claim flow; close for COD
+        if (payload.pay_mode === 'COD') {
+          closeDynCheckout();
+          alert(`Order created (COD). Order ID: #${data.order_id || 'N/A'}. We will dispatch shortly.`);
+          if (data.order_id) window.location.reload();
+          return;
+        }
+
         closeDynCheckout();
         // If server returned an immediate UPI page URL, open it
         if (data.upi_url) {
@@ -467,6 +482,32 @@
     }
   }
 
+  // Helper: open UPI QR page
+  window.openDynUpiQr = function() {
+    try {
+      const price = document.getElementById('dyn-prod-price').value || 0;
+      const url = `/api/debug/upi-qr?amount=${encodeURIComponent(price)}`;
+      window.open(url, '_blank');
+    } catch (e) { console.warn('openDynUpiQr error', e); }
+  };
+
+  // Helper: open QR scanner page
+  window.openDynUpiScanner = function() {
+    try {
+      const url = `/api/debug/qr-scanner`;
+      window.open(url, '_blank');
+    } catch (e) { console.warn('openDynUpiScanner error', e); }
+  };
+
+  // Toggle visibility of scanner buttons based on paymode
+  window.toggleDynScanner = function() {
+    try {
+      const paymode = document.getElementById('dyn-paymode').value;
+      const upiActions = document.getElementById('dyn-upi-actions');
+      if (upiActions) upiActions.style.display = (paymode === 'UPI') ? 'flex' : 'none';
+    } catch (e) {}
+  };
+
   if (document.readyState === 'loading') {
     document.addEventListener('DOMContentLoaded', syncProductsFromBackend);
   } else {
